# Backend WebSocket address. Defaults to ws://<hostname>:8000/ws in local development.
# Set in Vercel project Settings -> Environment Variables for remote production deployments:
VITE_WS_URL=wss://waretwin-backend.onrender.com/ws
